package revision.mocking;

public class BDDExample {

    /*
        BDD - Behaviour- Driven Development. Podejście to zaleca nam pisanie testów w języku naturalnym,
        który jest czytelny dla człowieka, jednocześnie koncentrując się na zachowaniu aplikacji.
        Podejście to definiuje ustrukturyzowany sposób pisania testów i określa trzy sekcje:
            • Arrange - zakładając pewne warunki początkowe (given),
            • Act - gdy wystąpi jakaś akcja (when),
            • Assert - zweryfikuj dane wyjściowe (then).

        BDD jest ewolucją TDD. BDD to metodologia tworzenia oprogramowania, w której aplikacja jest
        dokumentowana i zaprojektowana z myślą o zachowaniu, którego użytkownik oczekuje podczas
        interakcji z nią. Podejście to zaleca nam pisanie testów w języku naturalnym, który jest czytelny dla
        człowieka jednocześnie koncentrując się na zachowaniu aplikacji.

     */


}
